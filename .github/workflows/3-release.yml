name: 3. (CP2) Gerar Release e Tag

on:
  push:
    branches:
      - main

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Gerar Tag e Release
        uses: softprops/action-gh-release@v1
        with:
          generate_release_notes: true